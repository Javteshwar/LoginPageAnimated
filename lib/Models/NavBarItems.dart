import 'package:flutter/material.dart';

class CustomNavBarItems {
  Widget icon;
  Widget title;
  String label;
  Color bgColor;
  CustomNavBarItems({this.bgColor, this.icon, this.label, this.title});
}
